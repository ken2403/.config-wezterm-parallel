# =============================================================================
# WezTerm Parallel Development Setup
# =============================================================================
# Add this to your ~/.zshrc

# WezTerm CLI path (add to your path array)
# /Applications/WezTerm.app/Contents/MacOS(N-/)

# Parallel Development Commands
source ~/.config/zsh/parallel-dev.zsh
